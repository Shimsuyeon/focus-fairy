---
title: /end
description: End the current focus session.
sidebar:
  order: 2
---

Ends the active focus session.

```
/end
```

If another Slack app intercepts the command, use `/fend` as a drop-in alias. See [command conflict workaround](/en/commands/aliases/).

:::note
Translation in progress — see the [Korean version](/commands/end/).
:::
